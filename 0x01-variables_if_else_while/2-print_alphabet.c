for (i = 'a'; i <= 'z'; i++)_putchar(i);#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - prints if the number is positive negative or 0
 * Return: Always 0
 */
int main(void)
{
	char la,k;

	for (k = 0; k < 10; k++)
	{
	for (la = 'a'; la <= 'z'; la++)
	{
	_putchar(la);
	}
	_putchar('\n');
	}
}
